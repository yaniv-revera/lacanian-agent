{
  "name": "lacanian-agent",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "description": "A Lacanian analytic agent. The analyst ends the session; the door stays shut for 24 hours.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json && node dist/index.js",
    "test:guards": "tsc -p tsconfig.json && node dist/guards.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
