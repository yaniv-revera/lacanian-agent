{
  "name": "lacanian-agent",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "description": "A Lacanian analytic agent. The analyst ends the session; the door stays shut for 24 hours.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json && node dist/index.js",
    "test:guards": "tsc -p tsconfig.json && node dist/guards.test.js",
    "test:db": "tsc -p tsconfig.json && node dist/db.test.js",
    "test:migrations": "tsc -p tsconfig.json && node dist/migrations.test.js",
    "test": "npm run test:guards && npm run test:db && npm run test:migrations"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "nodemailer": "^9.0.5",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.9.0",
    "@types/nodemailer": "^8.0.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
